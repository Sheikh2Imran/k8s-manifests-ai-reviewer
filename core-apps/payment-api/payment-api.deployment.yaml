apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: web
        image: alpine
        command: ["sleep", "3600"]
